server {
    listen 8080;
    listen [::]:8080;
    server_name _;

    root /srv/progetti/hotel-os/frontend/dist;
    index index.html;

    location / {
        try_files $uri $uri/ /index.html;
    }
}
